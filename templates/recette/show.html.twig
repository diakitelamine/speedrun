{# {% extends 'base.html.twig' %}

{% block title %}Recette{% endblock %}

{% block body %}

 <div class="container">
    <h1>Détail de la recette</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recette.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ recette.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ recette.description }}</td>
            </tr>
            <tr>
                <th>Preparation</th>
                <td>{{ recette.preparation }}</td>
            </tr>
              <tr>
                <th>Ingredient</th>
                <td>{{ recette.ingredient}}</td>
            </tr>
            
            <tr>
                <th>Temps de Cuisson</th>
                <td>{{ recette.tempsCuisson }}</td>
            </tr>
            

            <tr>
                <th>Temps total</th>
                <td>{{ recette.tempsTotal }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ recette.photo }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('recette_index') }}">Revenir a la liste</a>

    <a href="{{ path('recette_edit', {'id': recette.id}) }}">edit</a>

    {{ include('recette/_delete_form.html.twig') }}
  </div>
{% endblock %} #}
