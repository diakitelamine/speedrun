{% extends 'base.html.twig' %}

{% block title %}Programe index{% endblock %}

{% block body %}
    <h1>Programe index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for programe in programes %}
            <tr>
                <td>{{ programe.id }}</td>
                <td>{{ programe.nom }}</td>
                <td>
                    <a href="{{ path('programe_show', {'id': programe.id}) }}">show</a>
                    <a href="{{ path('programe_edit', {'id': programe.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('programe_new') }}">Create new</a>
{% endblock %}
