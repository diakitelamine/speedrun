{% extends 'base.html.twig' %}

{% block title %}Programe{% endblock %}

{% block body %}
    <h1>Programe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ programe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ programe.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('programe_index') }}">back to list</a>

    <a href="{{ path('programe_edit', {'id': programe.id}) }}">edit</a>

    {{ include('programe/_delete_form.html.twig') }}
{% endblock %}
